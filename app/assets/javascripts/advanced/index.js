// app/assets/javascripts/advanced/index.js

//= require feature/advanced-search

$(document).ready(function() {

    // Only perform these actions on the appropriate pages.
    //if (!$('body').hasClass('advanced-page')) { return; }

    // ========================================================================
    // Actions
    // ========================================================================

    // None currently - all actions are through feature/advanced-search.

});
