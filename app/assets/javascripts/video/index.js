// app/assets/javascripts/video/index.js

//= require feature/video-popup

$(document).ready(function() {

    // Only perform these actions on the appropriate pages.
    //if (!$('body').hasClass('video-index-page')) { return; }

    // ========================================================================
    // Actions
    // ========================================================================

    // None currently - all actions are through feature/video-popup.

});
